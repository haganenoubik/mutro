<div class="container px-4 mx-auto">
  <h1 class="my-8 text-xl font-bold text-center text-primary border-b border-primary">MY GOOD VIBES</h1>

  <div class="grid grid-cols-1 gap-4 mb-16 md:grid-cols-4">
    <% @good_vibed_playlists.each do |playlist| %>
      <%= render 'playlists/playlist', playlist: playlist %>
    <% end %>
  </div>
</div>

<div class="flex justify-center">
  <%= paginate @good_vibed_playlists %>
</div>
